import React from 'react';
import { View } from 'react-native';
import { Text } from '../components';

export function MainReanimatedScreen(): React.ReactElement {
  return (
    <View>
      <Text>{'Main Reanimated Screen'}</Text>
    </View>
  );
}
