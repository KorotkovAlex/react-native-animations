import React from 'react';
import { View } from 'react-native';
import { Text } from '../components';

export function MainSkiaScreen(): React.ReactElement {
  return (
    <View>
      <Text>{'Main Skia Screen'}</Text>
    </View>
  );
}
